<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wkclz.micro.material.mapper.MdmMaterialTransferLogMapper">

    <select id="getByMaterialCode" resultType="com.wkclz.micro.material.bean.entity.MdmMaterialTransferLog">
        SELECT
            id, material_code, from_user_code, to_user_code, operator_code,
            user_code, tenant_code, sort, create_time, create_by, update_time, update_by, remark, version
        FROM mdm_material_transfer_log
        WHERE deleted = 0
            AND material_code = #{materialCode}
            <if test="tenantCode != null">AND tenant_code = #{tenantCode}</if>
        ORDER BY id DESC
    </select>

</mapper>
