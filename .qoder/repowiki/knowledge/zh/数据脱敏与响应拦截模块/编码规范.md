- 采用 DTO/Req/Resp 分层设计，请求参数封装在 `bean.req` 包，响应数据封装在 `bean.resp` 包，实体类位于 `bean.entity`。
- REST 接口统一继承 `Route` 常量类定义的路径前缀与方法后缀，保持 URL 结构的一致性。
- 服务层更新操作采用“先查后改”策略，通过 `copyIfNotNull` 合并变更字段，确保部分更新时的数据完整性。
- 缓存更新采用“本地内存 + Redis 信号量”模式，写操作更新 Redis 时间戳，读操作通过定时任务轮询 Redis 实现跨节点缓存失效。