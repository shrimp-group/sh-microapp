- 所有微信消息处理器均继承自 `AbstractHandler`，统一注入 SLF4J Logger 并实现 `WxMpMessageHandler` 接口。
- REST 接口路径统一定义在 `Route` 常量接口中，并通过 `@ApiDesc` 注解描述业务含义，保持接口文档与代码同步。
- 敏感配置字段（如 AppSecret、Token）在 Service 层查询返回前进行脱敏处理（替换为 `******`），防止密钥泄露。