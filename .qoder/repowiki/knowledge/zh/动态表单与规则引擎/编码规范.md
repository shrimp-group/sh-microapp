- DTO/Entity转换统一使用`BeanUtil.cp`或静态`copy`方法，避免手动setter赋值。
- REST接口返回值统一封装为`R<T>`泛型对象，分页数据使用`PageData<T>`包装。
- 业务唯一性校验在Service层通过`duplicateCheck`方法统一处理，冲突时抛出`ValidationException`。
- 缓存更新采用“写操作清除标记+定时异步加载”的模式，确保高并发下的读取性能。