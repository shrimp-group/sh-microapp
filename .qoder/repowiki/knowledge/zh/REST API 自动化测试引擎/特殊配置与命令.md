- **容器级 Mock 激活**：若需在应用启动阶段即替换 Mapper 等 Bean，需手动调用 `AutoMockBeanPostProcessor.enableMock()`。
- **报告持久化**：执行测试时通过 `POST /micro-autotest/run?reportDir=/path` 指定目录，模块会自动在该路径下生成带时间戳的 `.md` 和 `.html` 报告文件。