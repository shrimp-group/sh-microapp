- 测试数据生成遵循“最小有效值”原则，如数值类型默认为 1，布尔值为 true，字符串为空串，以确保接口调用的基本连通性。
- 在执行单个接口测试前后，通过 `mockHelper.mockControllerDependencies` 和 `resetAll` 确保 Mock 状态的隔离，防止不同接口测试间的副作用干扰。
- 接口描述信息优先从自定义注解 `@Desc` 或 `@ApiDesc` 中提取，若不存在则回退到 URI 路径转换生成的默认名称。