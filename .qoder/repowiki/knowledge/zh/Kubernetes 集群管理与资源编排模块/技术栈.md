- **Kubernetes Client**: 使用 `io.kubernetes:client-java` 进行 API 交互。
- **SSL/TLS 处理**: 手动构建 `OkHttpClient` 与 `SSLContext` 以支持 Base64/PKCS#8 格式的 Kubeconfig 证书认证。
- **代码生成**: 集成 `generator-client` 插件，支持通过 `gen_k8s` 选项自动生成基础 CRUD 代码。