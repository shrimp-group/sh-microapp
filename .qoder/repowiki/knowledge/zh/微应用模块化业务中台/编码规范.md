- 所有 REST 控制器必须继承或引用统一的 `Route` 常量类定义路径，禁止硬编码 URI。
- 请求对象（Req）必须根据场景继承 `sh-web` 提供的 `IdReq`、`PageReq` 或 `UpdateReq` 基类。
- 响应对象（Resp）若包含审计字段（如创建人、时间），必须继承 `sh-web` 的 `EntityResp` 基类。
- 每个模块必须在 `resources/META-INF/spring/` 下提供 `AutoConfiguration.imports` 文件以支持自动装配。