- REST 接口路径统一定义在 `Route` 常量接口中，并使用 `@Router` 和 `@ApiDesc` 注解进行模块化描述。
- 请求与响应对象严格区分，分别存放于 `bean.req` 和 `bean.resp` 包下，并通过 `BeanUtil.cp` 进行对象转换。
- 核心序列生成方法采用 `synchronized` 关键字配合数据库事务串行化隔离级别，以牺牲部分性能为代价确保强一致性。