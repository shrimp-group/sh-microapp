<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wkclz.micro.dbview.mapper.DbviewDatasourcePermissionMapper">

    <select id="getPermissionList" parameterType="com.wkclz.micro.dbview.bean.entity.DbviewDatasourcePermission" resultType="com.wkclz.micro.dbview.bean.entity.DbviewDatasourcePermission">
        SELECT
            id,
            datasource_id,
            user_code,
            permission_level,
            sort,
            create_time,
            create_by,
            update_time,
            update_by,
            remark,
            version
        FROM
            dbview_datasource_permission
        WHERE
            deleted = 0
            <if test="datasourceId != null">AND datasource_id = #{datasourceId}</if>
            <if test="userCode != null">AND user_code = #{userCode}</if>
            <if test="permissionLevel != null">AND permission_level = #{permissionLevel}</if>
        ORDER BY
            sort ASC,
            id ASC
    </select>

    <select id="selectByDatasourceIdAndUserCode" resultType="com.wkclz.micro.dbview.bean.entity.DbviewDatasourcePermission">
        SELECT
            id,
            datasource_id,
            user_code,
            permission_level,
            sort,
            create_time,
            create_by,
            update_time,
            update_by,
            remark,
            version
        FROM
            dbview_datasource_permission
        WHERE
            deleted = 0
            AND datasource_id = #{datasourceId}
            AND user_code = #{userCode}
        LIMIT 1
    </select>

</mapper>
