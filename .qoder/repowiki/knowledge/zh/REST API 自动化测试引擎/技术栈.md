- **测试模拟**：集成 `spring-test` (MockMvc) 进行 HTTP 请求模拟，使用 `mockito-core` 实现运行时 Bean 的动态 Mock。
- **外部 API 桩**：引入 `wiremock-standalone` (v3.13.1) 用于处理复杂的外部 HTTP 依赖场景。
- **序列化**：采用 `fastjson2` 进行请求体构建与响应数据的 JSON 处理。