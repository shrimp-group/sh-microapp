- 采用泛型方法设计（如 `<F extends BaseEntity> void create(F from)`），实现对任意继承自 BaseEntity 的实体进行无侵入式审计记录。
- 所有写操作（增删改）均支持可选的 `batchNo` 参数，若未提供则通过 `getBatchNo()` 自动生成，确保关联操作的原子性追踪。
- 在 `modify` 操作中强制校验 `from` 与 `to` 对象的 ID 一致性，防止因对象错位导致的审计数据污染。