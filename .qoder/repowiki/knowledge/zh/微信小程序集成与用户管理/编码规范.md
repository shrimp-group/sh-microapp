- REST 控制器统一继承 `Route` 接口定义的路径常量，并使用 `@Tag` 和 `@Operation` 注解完善 Swagger 文档。
- 服务层方法在处理用户信息更新时，采用选择性更新策略（`updateByIdSelective`），仅当字段值发生变化时才触发数据库更新。
- 所有请求参数对象均使用 `@Valid` 或自定义 Validator（如 `WxMaLoginFieldsValidator`）进行校验，确保入参合法性。