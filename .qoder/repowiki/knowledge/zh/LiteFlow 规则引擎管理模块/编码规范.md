- REST 接口路径统一定义在 `Route` 常量接口中，并使用 `@Router` 注解标识模块前缀。
- 实体类（Entity）内部提供静态 `copy` 与 `copyIfNotNull` 方法，用于对象属性的全量或选择性拷贝。
- Service 层统一继承 `BaseService<Entity, Mapper>`，复用通用的 CRUD 与分页查询能力。
- 请求与响应对象采用后缀命名规范，分别以 `Req` 和 `Resp` 结尾，并按功能细分（如 `PageReq`, `CreateReq`）。