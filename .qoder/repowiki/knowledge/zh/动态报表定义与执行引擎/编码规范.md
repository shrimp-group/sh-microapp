- REST 层统一通过 `Route` 接口常量定义 API 路径，并使用 `@ApiDesc` 标注业务含义。
- Service 层写操作（创建/更新/删除）强制添加 `@Transactional` 注解，并在事务内同步维护历史版本记录或清理缓存。
- 请求参数与响应数据严格隔离，分别定义在 `bean.req` 和 `bean.resp` 包下，通过 `BeanUtil.cp` 进行对象转换。
- SQL 辅助类采用正则表达式预处理清洗 MyBatis 动态标签（如 `<if>`, `<foreach>`），以支持纯 SQL 语法的静态解析。