<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wkclz.micro.material.mapper.MdmMaterialGroupMapper">

    <select id="getGroupTree" resultType="com.wkclz.micro.material.bean.entity.MdmMaterialGroup">
        SELECT
            id, group_code, parent_code, group_name, group_type,
            user_code, tenant_code, sort, create_time, create_by, update_time, update_by, remark, version
        FROM mdm_material_group
        WHERE deleted = 0
            AND tenant_code = #{tenantCode}
            AND (group_type = 'SYSTEM' OR user_code = #{userCode})
        ORDER BY sort ASC, id ASC
    </select>

    <select id="getGroups4Cache" resultType="com.wkclz.micro.material.bean.entity.MdmMaterialGroup">
        SELECT
            id, group_code, parent_code, group_name, group_type,
            user_code, tenant_code, sort, create_time, create_by, update_time, update_by, remark, version
        FROM mdm_material_group
        WHERE deleted = 0
        ORDER BY sort ASC, id ASC
    </select>

    <select id="getChildGroupCodes" resultType="com.wkclz.micro.material.bean.entity.MdmMaterialGroup">
        SELECT group_code, parent_code
        FROM mdm_material_group
        WHERE deleted = 0
            AND tenant_code = #{tenantCode}
            AND parent_code = #{parentCode}
        ORDER BY sort ASC, id ASC
    </select>

    <select id="getPickerGroupTree" resultType="com.wkclz.micro.material.bean.entity.MdmMaterialGroup">
        SELECT
            id, group_code, parent_code, group_name, group_type,
            user_code, tenant_code, sort, create_time, create_by, update_time, update_by, remark, version
        FROM mdm_material_group
        WHERE deleted = 0
            AND tenant_code = #{tenantCode}
            AND (group_type = 'SYSTEM' OR user_code = #{userCode})
        ORDER BY sort ASC, id ASC
    </select>

</mapper>
