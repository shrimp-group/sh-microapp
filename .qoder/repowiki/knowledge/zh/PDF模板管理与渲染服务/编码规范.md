- 采用 req/resp/entity 分离的 DTO 模式，在 `bean` 包下严格区分请求参数、响应结果与数据库实体。
- REST 控制器统一继承或引用 `Route` 常量类定义路径，并使用 `@Operation` 注解完善 Swagger/OpenAPI 文档描述。
- 服务层在进行新增或更新操作时，强制执行业务唯一键（如 templateCode）的重复性校验，冲突时抛出 `ValidationException`。