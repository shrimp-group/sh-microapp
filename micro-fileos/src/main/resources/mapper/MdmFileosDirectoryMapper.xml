<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wkclz.micro.fileos.mapper.MdmFileosDirectoryMapper">

    <select id="getDirectoryList" resultType="com.wkclz.micro.fileos.bean.entity.MdmFileosDirectory">
        SELECT
            id,
            tenant_code,
            bucket_name,
            dir_path,
            dir_name,
            parent_path,
            dir_level,
            file_count,
            total_size,
            sort,
            create_time,
            create_by,
            update_time,
            update_by,
            remark,
            version
        FROM
            mdm_fileos_directory
        WHERE
            deleted = 0
            AND parent_path = #{parentPath}
            <if test="bucketName != null">AND bucket_name = #{bucketName} </if>
            <if test="tenantCode != null">AND tenant_code = #{tenantCode} </if>
        ORDER BY
            dir_name ASC
    </select>

    <select id="getDirectoryTree" resultType="com.wkclz.micro.fileos.bean.entity.MdmFileosDirectory">
        SELECT
            id,
            tenant_code,
            bucket_name,
            dir_path,
            dir_name,
            parent_path,
            dir_level,
            file_count,
            total_size,
            sort,
            create_time,
            create_by,
            update_time,
            update_by,
            remark,
            version
        FROM
            mdm_fileos_directory
        WHERE
            deleted = 0
            <if test="bucketName != null">AND bucket_name = #{bucketName} </if>
            <if test="tenantCode != null">AND tenant_code = #{tenantCode} </if>
        ORDER BY
            dir_level ASC,
            dir_name ASC
    </select>

    <select id="getDirectoryByPath" resultType="com.wkclz.micro.fileos.bean.entity.MdmFileosDirectory">
        SELECT
            id,
            tenant_code,
            bucket_name,
            dir_path,
            dir_name,
            parent_path,
            dir_level,
            file_count,
            total_size,
            sort,
            create_time,
            create_by,
            update_time,
            update_by,
            remark,
            version
        FROM
            mdm_fileos_directory
        WHERE
            deleted = 0
            AND dir_path = #{dirPath}
            AND bucket_name = #{bucketName}
            <if test="tenantCode != null">AND tenant_code = #{tenantCode}</if>
    </select>

</mapper>
