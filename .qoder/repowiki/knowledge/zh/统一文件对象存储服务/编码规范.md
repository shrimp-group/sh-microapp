- API 接口与实现分离：核心业务逻辑定义在 `api` 包下的接口中（如 `FileosUploadApi`），并在 `api/impl` 包中提供具体实现，REST 控制器仅负责参数校验与委托调用。
- 统一异常处理：存储底层异常（如 `OSSException`）均被捕获并转换为框架统一的 `ValidationException`，确保对外抛出标准化的业务错误信息。
- 基于枚举的策略路由：通过 `OssSpEnum` 标识存储提供商类型，并利用 Spring 的 `Map<String, FileosService>` 注入特性，根据枚举对应的 Bean 名称动态获取服务实例。