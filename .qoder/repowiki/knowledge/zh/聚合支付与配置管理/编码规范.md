- REST 接口路径统一在 `Route` 接口中通过常量定义，并使用 `@ApiDesc` 标注业务含义，实现路由集中管理。
- 支付渠道客户端（Client）与配置信息采用懒加载策略存入静态 Map，并通过 Redis 键值变更触发本地缓存失效，实现多租户配置的动态刷新。
- 异步通知接口（Notify Rest）严格遵循“验签 -> 幂等检查 -> 业务处理 -> 更新状态 -> 回调 SPI”的处理链路，确保支付结果的可靠性。